/**
 * postinstall script – no patches currently required.
 * Add patches here if needed for future dependency upgrades.
 */

console.log("postinstall patches: 0 applied, 0 skipped (no patches needed)");
