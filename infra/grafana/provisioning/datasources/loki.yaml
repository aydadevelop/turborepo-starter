apiVersion: 1

datasources:
  - name: Loki
    type: loki
    uid: loki          # referenced by alert-rules.yaml — must not change
    access: proxy
    url: http://loki:3100
    isDefault: true
    version: 1
    editable: false
