---
phase: 09-operator-catalog-booking-intake-wiring
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/src/routes/(app)/org/+layout.svelte
  - apps/web/src/routes/(app)/org/listings/+page.svelte
  - apps/web/src/routes/(app)/org/listings/new/+page.svelte
  - apps/web/src/routes/(app)/org/listings/[id]/+page.svelte
  - apps/web/src/components/org/ListingEditorForm.svelte
  - apps/web/src/components/org/ListingPublicationButton.svelte
autonomous: true
requirements:
  - AUTH-01
  - CATL-01
  - CATL-02

must_haves:
  truths:
    - "Authenticated operators with an active organization can open `/org/listings` and see only their organization’s listings."
    - "Operators can create and update listings from `apps/web` using the live typed listing API instead of API-only smoke checks."
    - "Operators can publish and unpublish listings from the live app, and the page refreshes to reflect the new listing state."
  artifacts:
    - path: apps/web/src/routes/(app)/org/listings/+page.svelte
      provides: operator listings index backed by `orpc.listing.list`
    - path: apps/web/src/routes/(app)/org/listings/new/+page.svelte
      provides: live create-listing route in the org shell
    - path: apps/web/src/routes/(app)/org/listings/[id]/+page.svelte
      provides: live edit-listing route in the org shell
    - path: apps/web/src/components/org/ListingEditorForm.svelte
      provides: shared create/edit listing form with client-side JSON metadata validation
    - path: apps/web/src/components/org/ListingPublicationButton.svelte
      provides: self-contained publish/unpublish mutation control
  key_links:
    - from: apps/web/src/routes/(app)/org/listings/+page.svelte
      to: packages/api via /rpc
      via: createQuery(() => orpc.listing.list.queryOptions({ input: { limit, offset } }))
      pattern: orpc\.listing\.list\.queryOptions
    - from: apps/web/src/components/org/ListingEditorForm.svelte
      to: packages/api via /rpc
      via: createMutation(() => orpc.listing.create/update.mutationOptions(...))
      pattern: orpc\.listing\.(create|update)\.mutationOptions
    - from: apps/web/src/components/org/ListingPublicationButton.svelte
      to: packages/api via /rpc
      via: createMutation(() => orpc.listing.publish/unpublish.mutationOptions(...))
      pattern: orpc\.listing\.(publish|unpublish)\.mutationOptions
---

<objective>
Deliver the missing operator listing-management flow in `apps/web` so signed-in organization managers can create, edit, publish, and unpublish listings from the live app.

Purpose: Phase 03 built the catalog domain package and typed listing transport, but the milestone audit still marks `AUTH-01`, `CATL-01`, and `CATL-02` unsatisfied because operators have no live app surface consuming those APIs.
Output: A new `/org/listings` management area with live create/edit routes and publish/unpublish controls.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/v1.0-v1.0-MILESTONE-AUDIT.md
@.planning/phases/03-org-access-catalog-storefront/03-02-SUMMARY.md
@apps/web/src/routes/(app)/org/+layout.svelte
@apps/web/src/routes/(app)/org/+page.svelte
@apps/web/src/lib/orpc.ts
@packages/api-contract/src/routers/listing.ts

<interfaces>
From `apps/web/src/routes/(app)/org/+layout.svelte`:
- org management pages already require an authenticated session
- access is guarded by `orpc.canManageOrganization.queryOptions()`
- tab navigation is the repo-native extension point for new operator surfaces

From `packages/api-contract/src/routers/listing.ts`:
```typescript
create.input = {
  listingTypeSlug: string;
  name: string;
  slug: string;
  description?: string;
  metadata?: Record<string, unknown>;
  timezone?: string;
}

update.input = {
  id: string;
  name?: string;
  description?: string;
  metadata?: Record<string, unknown>;
  timezone?: string;
}

list.input = { limit: number; offset: number }
publish.input = { id: string; channelType?: "own_site" | "platform_marketplace" }
unpublish.input = { id: string }
```

From `packages/api/src/handlers/listing.ts`:
- handlers already derive `organizationId` from `context.activeMembership.organizationId`
- publish defaults can remain platform-marketplace scoped for this phase; no extra channel selector is required
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the org listings index and live publish/unpublish controls</name>
  <files>
    apps/web/src/routes/(app)/org/+layout.svelte
    apps/web/src/routes/(app)/org/listings/+page.svelte
    apps/web/src/components/org/ListingPublicationButton.svelte
  </files>
  <action>
Extend the `/org` navigation with a `Listings` tab for users who already pass the existing `canManageOrganization` guard.

Create `apps/web/src/routes/(app)/org/listings/+page.svelte` as the operator index page. Use `createQuery(() => orpc.listing.list.queryOptions({ input: { limit: 50, offset: 0 } }))` and render a simple management list with each row/card showing:
- listing name
- slug
- `listingTypeSlug`
- status badge derived from `status` / `isActive`
- edit link
- publish/unpublish action

Create `apps/web/src/components/org/ListingPublicationButton.svelte` as a self-contained action component. It should:
- accept the listing id + current published state as props
- call `orpc.listing.publish` or `orpc.listing.unpublish` via `createMutation`
- invalidate `orpc.listing.key()` on success
- default publish to the existing platform-marketplace channel (do not add a channel chooser in this phase)
- show pending/error state inline instead of logging-only behavior

Do not create a new dashboard namespace. Keep the entire flow under the existing `/org` shell.
  </action>
  <verify>
    <automated>cd /Users/d/Documents/Projects/turborepo-alchemy && bun run check-types --filter=web 2>&1 | tail -30</automated>
  </verify>
  <done>
    Operators can reach `/org/listings`, see their organization’s listings, and trigger publish/unpublish against the live typed API.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add create/edit listing routes with a shared editor form</name>
  <files>
    apps/web/src/routes/(app)/org/listings/new/+page.svelte
    apps/web/src/routes/(app)/org/listings/[id]/+page.svelte
    apps/web/src/components/org/ListingEditorForm.svelte
  </files>
  <action>
Create `apps/web/src/components/org/ListingEditorForm.svelte` as the shared operator form for both create and edit flows. Use the current listing contract as-is; the form must cover:
- `listingTypeSlug`
- `name`
- `slug`
- `timezone`
- `description`
- `metadata` edited as JSON text with client-side parse validation before mutation

`new/+page.svelte` should use the shared form with `orpc.listing.create`.

`[id]/+page.svelte` should:
- load the listing via `orpc.listing.get.queryOptions({ input: { id } })`
- seed the shared form from query data
- submit via `orpc.listing.update`

For both routes:
- invalidate `orpc.listing.key()` after success
- navigate back to `/org/listings` after a successful mutation
- keep mutations inline with the component/page that triggers them; do not add a new generic form framework for this phase
- do not add a new listing-type discovery endpoint; use the existing contract shape directly
  </action>
  <verify>
    <automated>cd /Users/d/Documents/Projects/turborepo-alchemy && bun run check-types --filter=web 2>&1 | tail -30</automated>
  </verify>
  <done>
    Operators can create and edit listings from the live app, with metadata validated client-side and persisted through the existing typed listing API.
  </done>
</task>

</tasks>

<verification>
1. `bun run check-types --filter=web` passes after the new `/org/listings` pages and components are added.
2. `/org/+layout.svelte` exposes a `Listings` tab alongside the existing org-management tabs.
3. The new routes and components all use `orpc.listing.*` rather than bespoke fetch calls.
</verification>

<success_criteria>
- `/org/listings` exists inside the existing authenticated org shell.
- Operators can create and edit listings through `apps/web`.
- Operators can publish and unpublish listings through `apps/web`.
- The UI uses the current typed listing contract and invalidates the correct oRPC cache keys.
</success_criteria>

<output>
After completion, create `.planning/phases/09-operator-catalog-booking-intake-wiring/09-01-SUMMARY.md`
</output>
