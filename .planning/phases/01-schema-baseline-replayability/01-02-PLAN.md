---
phase: 01-schema-baseline-replayability
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - packages/db/scripts/seed-local.mjs
  - packages/db/scripts/cleanup-tables.mjs
  - packages/db/src/test/index.ts
  - packages/db/src/__tests__/database.test.ts
  - packages/db/src/test/fixtures/
autonomous: true
requirements:
  - PLAT-02
must_haves:
  truths:
    - Deterministic seed data covers representative marketplace scenarios instead of starter-only entities.
    - DB tests and bootstrap flows can reuse the same scenario definitions.
  artifacts:
    - packages/db/scripts/seed-local.mjs
    - packages/db/src/test/index.ts
    - packages/db/src/test/fixtures/
    - packages/db/src/__tests__/database.test.ts
  key_links:
    - Seed scenario builders are shared between CLI seeding and DB tests.
    - Anchor-date determinism survives repeated seed runs.
---

<objective>
Turn the existing seed path into a reusable, deterministic marketplace scenario system that supports both local development and future brownfield parity work.

Purpose: Make representative data state cheap to reproduce and verify.
Output: Scenario-driven seeds plus reusable fixture builders wired into DB tests.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-schema-baseline-replayability/01-RESEARCH.md
@packages/db/scripts/seed-local.mjs
@packages/db/scripts/cleanup-tables.mjs
@packages/db/src/test/index.ts
@packages/db/src/__tests__/database.test.ts

<interfaces>
From `packages/db/scripts/seed-local.mjs`:
- `--anchor-date <YYYY-MM-DD>` already exists and should stay deterministic.
- Current seed data is concentrated in auth, notifications, assistant, and demo todo records.

From `packages/db/src/test/index.ts`:
- `bootstrapTestDatabase({ seed, seedStrategy })` is the reusable fast-path harness.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract reusable marketplace seed scenarios from the monolithic seed script</name>
  <files>
packages/db/scripts/seed-local.mjs
packages/db/scripts/cleanup-tables.mjs
packages/db/src/test/fixtures/
  </files>
  <action>Refactor the local seed path into reusable scenario builders focused on representative marketplace states: organization, listing, availability, booking, payment, and support. Keep deterministic IDs and anchor-date control. Do not let demo `todo` rows remain the primary proof that seeding works; they can stay, but marketplace scenarios must become first-class.</action>
  <verify>
    <automated>docker compose up -d db && cd packages/db && DATABASE_URL=postgresql://postgres:postgres@127.0.0.1:5432/myapp bun run db:seed -- --anchor-date 2026-03-15</automated>
  </verify>
  <done>The seed path produces stable marketplace baseline scenarios that can be re-run without manual cleanup ambiguity.</done>
</task>

<task type="auto">
  <name>Task 2: Reuse the same scenario builders in the DB test harness</name>
  <files>
packages/db/src/test/index.ts
packages/db/src/__tests__/database.test.ts
packages/db/src/test/fixtures/
  </files>
  <action>Wire the new scenario builders into `bootstrapTestDatabase` consumers and extend DB tests so they assert representative marketplace fixture behavior, not only starter/auth/demo semantics. Prefer TDD-managed additions: write the failing assertions for the new scenarios first, then make the smallest fixture-builder changes to satisfy them.</action>
  <verify>
    <automated>cd packages/db && bun run test</automated>
  </verify>
  <done>DB tests can boot into deterministic marketplace fixture states through shared builders rather than hand-written per-test setup.</done>
</task>

<task type="auto">
  <name>Task 3: Keep local and e2e seed entrypoints aligned on one deterministic baseline</name>
  <files>
packages/db/scripts/seed-local.mjs
packages/db/scripts/bootstrap-local-e2e.mjs
packages/db/package.json
  </files>
  <action>Align the package scripts and bootstrap entrypoints so local development and e2e setup resolve to the same deterministic marketplace baseline and anchor-date behavior. Keep the task logic package-owned and avoid introducing a second independent seed path.</action>
  <verify>
    <automated>docker compose up -d db && cd packages/db && PLAYWRIGHT_SEED_ANCHOR_DATE=2026-03-15 bun run db:bootstrap:local:e2e</automated>
  </verify>
  <done>There is one deterministic seed baseline reused by local scripts and e2e bootstrap, with stable anchor-date behavior.</done>
</task>

</tasks>

<verification>
- Confirm deterministic seed builders exist outside the monolithic inline seed blob.
- Confirm DB tests assert marketplace scenarios, not only auth/todo primitives.
- Confirm e2e bootstrap and local seeding share the same baseline data story.
</verification>

<success_criteria>
A developer can seed representative marketplace scenarios repeatedly with the same results, and tests can reuse those scenarios instead of rebuilding them ad hoc.
</success_criteria>

<output>
After completion, create `.planning/phases/01-schema-baseline-replayability/01-02-SUMMARY.md`
</output>