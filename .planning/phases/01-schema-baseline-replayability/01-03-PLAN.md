---
phase: 01-schema-baseline-replayability
plan: 03
type: execute
wave: 3
depends_on:
  - 01-01
  - 01-02
files_modified:
  - packages/db/scripts/export-snapshot.mjs
  - packages/db/scripts/import-snapshot.mjs
  - packages/db/scripts/verify-postgres-baseline.mjs
  - packages/db/package.json
  - packages/db/snapshots/
  - docker-compose.e2e.yml
autonomous: true
requirements:
  - PLAT-03
  - PLAT-04
must_haves:
  truths:
    - Replayable baseline snapshots can be exported and restored without hand-built SQL surgery.
    - The team has a real-Postgres verification lane for baseline migration, seed, and restore behavior.
  artifacts:
    - packages/db/scripts/export-snapshot.mjs
    - packages/db/scripts/import-snapshot.mjs
    - packages/db/scripts/verify-postgres-baseline.mjs
    - packages/db/snapshots/
    - packages/db/package.json
  key_links:
    - Snapshot restore runs after migrate and seed against real Postgres.
    - Docker Postgres verification complements rather than replaces PGlite tests.
---

<objective>
Add replayable baseline snapshots and a real-Postgres verification lane so Phase 1 ends with reproducible data-state recovery, not just migration optimism.

Purpose: Make future planning and execution safe, debuggable, and demonstrably portable across environments.
Output: Snapshot CLI scripts, committed baseline snapshot artifact(s), and a real-Postgres verification command.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-schema-baseline-replayability/01-RESEARCH.md
@packages/db/scripts/bootstrap-local-e2e.mjs
@packages/db/src/test/index.ts
@docker-compose.yml
@docker-compose.e2e.yml

<interfaces>
From `packages/db/scripts/bootstrap-local-e2e.mjs`:
- Current behavior is wait-for-db → apply schema → run seed with anchor date.

From `packages/db/src/test/index.ts`:
- PGlite remains the fast feedback harness and should not be removed.

From `docker-compose.e2e.yml`:
- Real Docker Postgres already exists as a CI/local-friendly service shape.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add repo-native snapshot export and restore scripts for the baseline marketplace state</name>
  <files>
packages/db/scripts/export-snapshot.mjs
packages/db/scripts/import-snapshot.mjs
packages/db/snapshots/
packages/db/package.json
  </files>
  <action>Create package-local CLI scripts that export and restore a deterministic baseline marketplace state in a committed, replayable format. Prefer a repo-native JSON/structured snapshot approach that works through `pg` and package scripts, rather than relying on undocumented manual pg_dump/psql steps. Make restore idempotent enough for repeatable debugging and parity work.</action>
  <verify>
    <automated>docker compose up -d db && cd packages/db && DATABASE_URL=postgresql://postgres:postgres@127.0.0.1:5432/myapp node ./scripts/export-snapshot.mjs --out ./snapshots/phase1-baseline.json && node ./scripts/import-snapshot.mjs --file ./snapshots/phase1-baseline.json</automated>
  </verify>
  <done>The repo contains a committed baseline snapshot format and scripts that can export and restore it predictably.</done>
</task>

<task type="auto">
  <name>Task 2: Add a real-Postgres baseline verification command</name>
  <files>
packages/db/scripts/verify-postgres-baseline.mjs
packages/db/package.json
docker-compose.e2e.yml
  </files>
  <action>Create a package-owned verification command that exercises the full real-Postgres baseline flow: start/target Docker Postgres, reset if needed, migrate, seed, restore snapshot, and assert that the baseline state is sane. Keep PGlite as the fast lane; this new command is the acceptance lane for migration order, restore flow, and extension/invariant-sensitive behavior.</action>
  <verify>
    <automated>docker compose up -d db && cd packages/db && DATABASE_URL=postgresql://postgres:postgres@127.0.0.1:5432/myapp node ./scripts/verify-postgres-baseline.mjs</automated>
  </verify>
  <done>The team can run one package-local command to prove the baseline works against real Postgres before moving to Phase 2.</done>
</task>

<task type="auto">
  <name>Task 3: Wire snapshot and verification behavior into the routine package workflow</name>
  <files>
packages/db/package.json
packages/db/scripts/verify-postgres-baseline.mjs
packages/db/snapshots/
  </files>
  <action>Add clearly named package scripts for snapshot export/import and real-Postgres verification so future phases can depend on them without tribal knowledge. Keep these as package tasks registered through Turborepo conventions; do not hide the flow in root-level custom task logic.</action>
  <verify>
    <automated>cd packages/db && bun run check-types && bun run test && bun run db:verify:postgres</automated>
  </verify>
  <done>Snapshot restore and real-Postgres verification are routine, discoverable parts of the DB package workflow.</done>
</task>

</tasks>

<verification>
- Confirm a committed snapshot artifact exists under `packages/db/snapshots/`.
- Confirm the verification lane uses real Docker Postgres rather than only PGlite.
- Confirm package scripts expose export, import, and verify flows clearly.
</verification>

<success_criteria>
A developer can restore a committed baseline snapshot and prove the full baseline path works against real Postgres, giving later phases a reproducible data state to build on.
</success_criteria>

<output>
After completion, create `.planning/phases/01-schema-baseline-replayability/01-03-SUMMARY.md`
</output>