---
phase: 01-schema-baseline-replayability
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/db/src/migrations/
  - packages/db/drizzle.config.ts
  - packages/db/drizzle.config.dev.ts
  - packages/db/package.json
  - packages/db/scripts/bootstrap-local-e2e.mjs
  - packages/db/src/triggers.ts
autonomous: true
requirements:
  - PLAT-01
must_haves:
  truths:
    - A clean database can be recreated from committed migration artifacts without manual repair.
    - Local and e2e bootstrap flows use the same baseline migration path instead of ad hoc schema push behavior.
  artifacts:
    - packages/db/src/migrations/
    - packages/db/package.json
    - packages/db/scripts/bootstrap-local-e2e.mjs
    - packages/db/src/triggers.ts
  key_links:
    - drizzle configs and package scripts resolve the same migration directory.
    - e2e bootstrap applies migrations before seeding.
---

<objective>
Lock the current exported Drizzle schema into a committed baseline migration path and make reproducible bootstrap the default for environments that matter.

Purpose: Make schema state replayable before any domain extraction begins.
Output: Committed migration artifacts plus migration-backed bootstrap scripts.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-schema-baseline-replayability/01-RESEARCH.md
@packages/db/package.json
@packages/db/drizzle.config.ts
@packages/db/drizzle.config.dev.ts
@packages/db/scripts/bootstrap-local-e2e.mjs
@packages/db/src/triggers.ts
@packages/db/src/schema/index.ts
@packages/db/src/schema/marketplace.ts

<interfaces>
From `packages/db/package.json`:
- `db:push` → `drizzle-kit push`
- `db:migrate` → `drizzle-kit migrate`
- `db:generate` → `drizzle-kit generate`
- `db:bootstrap:local:e2e` → `node ./scripts/bootstrap-local-e2e.mjs`

From `packages/db/src/schema/marketplace.ts`:
- Extension-backed search/index fields are intentionally omitted until the test harness supports extension-aware Postgres.

From `packages/db/src/triggers.ts`:
- `POST_MIGRATION_TRIGGERS_SQL` is the stable hook point for future trigger/function SQL.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Commit the baseline migration chain from the current exported schema</name>
  <files>
packages/db/src/migrations/
packages/db/drizzle.config.ts
packages/db/drizzle.config.dev.ts
  </files>
  <action>Generate and commit the first migration chain from the current exported schema in `packages/db/src/schema/index.ts`. Normalize both Drizzle configs so they point at the same committed migration directory and use the current repo env-loading conventions consistently. Preserve the explicit deferral in `packages/db/src/schema/marketplace.ts`: do NOT add pgvector, pg_textsearch, earthdistance, or other extension-dependent fields/indexes in this plan.</action>
  <verify>
    <automated>docker compose up -d db && cd packages/db && DATABASE_URL=postgresql://postgres:postgres@127.0.0.1:5432/myapp bun run db:migrate</automated>
  </verify>
  <done>A clean Docker Postgres instance can apply the committed baseline migrations without manual fixes.</done>
</task>

<task type="auto">
  <name>Task 2: Make migration-backed bootstrap the default reproducible path</name>
  <files>
packages/db/package.json
packages/db/scripts/bootstrap-local-e2e.mjs
packages/db/src/triggers.ts
  </files>
  <action>Replace push-first bootstrap behavior with migration-backed bootstrap for the local/e2e path. Expose or align package scripts so the reproducible flow is reset → migrate → seed, and keep a stable post-migration SQL hook through `POST_MIGRATION_TRIGGERS_SQL` for future trigger/function work. Keep the PGlite unit-test harness intact; do not force migration files through the in-memory fast path.</action>
  <verify>
    <automated>docker compose up -d db && cd packages/db && DATABASE_URL=postgresql://postgres:postgres@127.0.0.1:5432/myapp bun run db:bootstrap:local:e2e -- --anchor-date 2026-03-15</automated>
  </verify>
  <done>The repo has one explicit, migration-backed bootstrap path for real Postgres environments and a preserved hook for future post-migration SQL.</done>
</task>

<task type="auto">
  <name>Task 3: Add a migration smoke check to the DB package verification path</name>
  <files>
packages/db/package.json
packages/db/src/__tests__/
  </files>
  <action>Add a package-local smoke verification step that proves the baseline migration path works alongside existing DB checks. Keep it lightweight and package-owned — do not move task logic to the root package. This task should verify the migration baseline exists and is invocable, not broaden Phase 1 into later domain semantics.</action>
  <verify>
    <automated>cd packages/db && bun run check-types && bun run test</automated>
  </verify>
  <done>The DB package exposes a routine verification path that covers the baseline migration work without relying on undocumented manual steps.</done>
</task>

</tasks>

<verification>
- Confirm `packages/db/src/migrations/` is committed and no longer implicit-only.
- Confirm `bootstrap-local-e2e.mjs` no longer depends on `drizzle-kit push` as the reproducibility path.
- Confirm package-owned scripts remain aligned with Turborepo package-task conventions.
</verification>

<success_criteria>
A clean database can be rebuilt from committed migrations, and the real-Postgres bootstrap path uses those migrations before any seeds or snapshots are applied.
</success_criteria>

<output>
After completion, create `.planning/phases/01-schema-baseline-replayability/01-01-SUMMARY.md`
</output>