---
phase: 10-payment-webhook-cancellation-live-path
plan: "04"
type: tdd
wave: 3
depends_on:
  - 10-03
files_modified:
  - packages/api/package.json
  - packages/api/src/handlers/booking.ts
  - packages/api/src/__tests__/booking-cancellation.test.ts
autonomous: true
requirements:
  - BOOK-05
must_haves:
  truths:
    - "The live `booking.applyCancellation` API surface delegates to the disputes workflow instead of calling the old booking-local `applyCancellation()` service directly."
    - "An API-level cancellation apply request marks the booking/request state through the disputes workflow and returns the resulting `requestId` + `refundId` shape expected by the contract."
    - "API-level tests prove the live handler path uses the same refund/orchestration behavior hidden tests will exercise, not a scaffold-only helper."
  artifacts:
    - path: packages/api/src/handlers/booking.ts
      provides: thin live apply handler that executes the disputes workflow with workflow/event context
      exports:
        - bookingRouter
    - path: packages/api/src/__tests__/booking-cancellation.test.ts
      provides: API-level regression coverage for live cancellation apply behavior
    - path: packages/api/package.json
      provides: direct runtime dependency wiring for `@my-app/disputes` and `@my-app/events`
  key_links:
    - from: packages/api/src/handlers/booking.ts
      to: packages/disputes/src/cancellation-workflow.ts
      via: processCancellationWorkflow(db).execute(...)
      pattern: processCancellationWorkflow
    - from: packages/api/src/handlers/booking.ts
      to: packages/events/src/event-bus.ts
      via: workflow context event bus construction at the transport edge
      pattern: new EventBus
    - from: packages/api/src/__tests__/booking-cancellation.test.ts
      to: '@my-app/payment provider registry'
      via: fake provider registration proving the live handler path exercises workflow refund execution
      pattern: registerPaymentProvider|resetPaymentProviderRegistry
---

<objective>
Finish the live cancellation path by wiring `booking.applyCancellation` to the disputes workflow and proving at the API layer that the production mutation path now uses the real orchestration/refund flow.

Purpose: BOOK-05 is not satisfied until the live API surface stops calling the booking-local scaffold and actually runs the disputes workflow path hidden tests will hit.
Output: RED→GREEN booking handler change plus API-level cancellation apply coverage.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-payments-notifications-support/06-03-SUMMARY.md
@.planning/phases/10-payment-webhook-cancellation-live-path/10-RESEARCH.md
@packages/api/src/handlers/booking.ts
@packages/api/package.json
@packages/api-contract/src/routers/booking.ts
@packages/disputes/src/cancellation-workflow.ts
@packages/events/src/event-bus.ts
@packages/events/src/types.ts

<interfaces>
Current live handler path in `packages/api/src/handlers/booking.ts`:
```typescript
const { request, refundId } = await applyCancellation(
  input.requestId,
  context.activeMembership.organizationId,
  context.session?.user?.id ?? "system",
  db,
);
return { requestId: request.id, refundId };
```

Workflow execution shape from `packages/workflows`:
```typescript
const result = await workflow.execute(input, {
  organizationId,
  actorUserId,
  idempotencyKey,
  eventBus,
});
```

Existing contract shape to preserve:
```typescript
applyCancellation.output({
  requestId: z.string(),
  refundId: z.string().nullable(),
})
```
</interfaces>
</context>

<feature>
  <name>booking.applyCancellation uses the disputes workflow on the live API path</name>
  <files>
    packages/api/package.json
    packages/api/src/handlers/booking.ts
    packages/api/src/__tests__/booking-cancellation.test.ts
  </files>
  <behavior>
    Write API-level failing tests first, then make them pass.

    RED coverage to add before implementation:
    - successful apply request through the live booking handler returns `{ requestId, refundId }`, updates booking/request state, and uses a registered fake payment provider when the stored request has a non-zero refund amount
    - zero-refund request still succeeds through the workflow path without calling the provider
    - missing request / invalid request state is translated to the existing ORPC-style `NOT_FOUND` / `BAD_REQUEST` outcomes

    GREEN implementation requirements:
    1. Add direct dependencies in `packages/api/package.json` for the packages needed by the live handler (`@my-app/disputes` and `@my-app/events`).
    2. In `packages/api/src/handlers/booking.ts`, stop calling `@my-app/booking.applyCancellation` from `booking.applyCancellation`.
    3. Replace that path with `processCancellationWorkflow(db).execute(...)`, passing a workflow context constructed at the transport edge:
       - `organizationId: context.activeMembership.organizationId`
       - `actorUserId: context.session?.user?.id`
       - `idempotencyKey` derived from the request id
       - `eventBus: new EventBus(context.notificationQueue)`
    4. Preserve the contract output shape (`{ requestId, refundId }`) and current ORPC error translations for missing/invalid requests.
    5. If the workflow returns an execution failure result, translate that back into the original domain error semantics instead of swallowing it.

    Do not change the booking contract schema unless absolutely necessary. This plan is about live-path adoption, not a public API redesign.

    TDD commits:
    - `test(10-04): add failing live cancellation handler coverage`
    - `feat(10-04): wire booking applyCancellation to disputes workflow`
  </behavior>
  <implementation>
    Keep the handler thin:
    - it builds workflow context
    - it delegates to the disputes workflow
    - it translates domain/workflow errors into ORPC responses

    Do not duplicate refund logic in the handler and do not reintroduce direct DB mutation there. The owning orchestration layer for apply-time cancellation belongs in `packages/disputes`.
  </implementation>
</feature>

<verification>
- `cd /Users/d/Documents/Projects/turborepo-alchemy/packages/api && bunx vitest run src/__tests__/booking-cancellation.test.ts && bun run check-types`
- `cd /Users/d/Documents/Projects/turborepo-alchemy/packages/disputes && bun run check-types`
</verification>

<success_criteria>
- The live booking apply handler no longer calls the booking-local scaffold service.
- API-level tests prove the live path executes the disputes workflow and refund boundary.
- The public contract output and ORPC error semantics stay stable while the owning layer changes underneath.
</success_criteria>

<output>
After completion, create `.planning/phases/10-payment-webhook-cancellation-live-path/10-04-SUMMARY.md`
</output>
