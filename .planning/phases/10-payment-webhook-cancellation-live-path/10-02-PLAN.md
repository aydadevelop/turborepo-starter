---
phase: 10-payment-webhook-cancellation-live-path
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/payment/src/provider.ts
  - packages/payment/src/registry.ts
  - packages/payment/src/adapters/cloudpayments.ts
  - packages/payment/src/__tests__/cloudpayments-provider.test.ts
  - packages/payment/src/index.ts
autonomous: true
requirements:
  - BOOK-05
must_haves:
  truths:
    - "Refund execution can be invoked through a resettable payment-provider registry instead of hard-coding CloudPayments fetch logic inside disputes or API handlers."
    - "Execution-side payment provider methods accept org-scoped credential/config data at call time instead of reading env vars inside domain methods."
    - "CloudPayments refund requests convert integer cents to provider decimal amounts only at the adapter boundary and fail explicitly on malformed credentials or provider errors."
  artifacts:
    - path: packages/payment/src/provider.ts
      provides: execution-side payment provider contract and per-call config shape for refunds
      exports:
        - PaymentProvider
        - PaymentExecutionConfig
        - RefundPaymentInput
    - path: packages/payment/src/registry.ts
      provides: register/get/reset helpers for payment execution providers
      exports:
        - registerPaymentProvider
        - getPaymentProvider
        - resetPaymentProviderRegistry
    - path: packages/payment/src/adapters/cloudpayments.ts
      provides: CloudPayments refund adapter using org-scoped credentials passed at call time
      exports:
        - CloudPaymentsPaymentProvider
        - createCloudPaymentsPaymentProvider
    - path: packages/payment/src/__tests__/cloudpayments-provider.test.ts
      provides: coverage for registry resolution, credential parsing, and refund request error handling
  key_links:
    - from: packages/payment/src/adapters/cloudpayments.ts
      to: docs/CloudPayments.md
      via: refund API call and Basic Auth request construction
      pattern: refund|Authorization
    - from: packages/payment/src/registry.ts
      to: packages/disputes/src/cancellation-workflow.ts
      via: execution-side provider lookup by payment config provider id
      pattern: getPaymentProvider
    - from: packages/payment/src/provider.ts
      to: packages/calendar/src/types.ts
      via: per-call config pattern reused from calendar adapter boundaries
      pattern: credentials are stored encrypted in the DB and passed in at call time
---

<objective>
Add the execution-side payment provider boundary that Phase 10 needs for real refunds, following the repo’s existing adapter/registry style and the calendar package’s “DB credentials passed at call time” pattern.

Purpose: The disputes workflow can’t execute a live refund until `@my-app/payment` exposes a swappable provider registry that accepts org-scoped credentials at runtime.
Output: New payment provider interface + registry + CloudPayments refund adapter + focused adapter tests.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-review-missing-extractions/07-04-SUMMARY.md
@.planning/phases/10-payment-webhook-cancellation-live-path/10-RESEARCH.md
@packages/payment/src/index.ts
@packages/calendar/src/types.ts
@packages/calendar/src/google-adapter.ts
@packages/db/src/schema/marketplace.ts
@docs/CloudPayments.md

<interfaces>
Current `@my-app/payment` public surface in `packages/payment/src/index.ts`:
```typescript
export * from "./types";
export * from "./payment-service";
```

Reusable per-call credential pattern from `packages/calendar/src/types.ts`:
```typescript
export interface CalendarConnectionConfig {
  provider: CalendarAdapterProvider;
  credentials: Record<string, unknown>;
  calendarId: string;
}
```

Relevant org payment config fields from `packages/db/src/schema/marketplace.ts`:
- `provider`
- `publicKey`
- `encryptedCredentials`
- `credentialKeyVersion`

Phase 10 constraint from research:
- do not invent a general KMS/decryption subsystem in this phase
- keep credentials org-scoped and passed at call time
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define the payment execution contract and resettable registry</name>
  <files>
    packages/payment/src/provider.ts
    packages/payment/src/registry.ts
    packages/payment/src/index.ts
    packages/payment/src/__tests__/cloudpayments-provider.test.ts
  </files>
  <action>
Create the execution-side payment boundary in `@my-app/payment`.

Add `packages/payment/src/provider.ts` with the minimal Phase 10 contract for refunds:
- `PaymentExecutionConfig` with org-scoped data passed at call time (provider id, optional public key, credentials blob parsed into `Record<string, unknown>`)
- `RefundPaymentInput` with integer-cent amount, provider payment id / transaction id, currency, and idempotency key
- `PaymentProvider` interface exposing `refundPayment(input, config)` and returning `{ externalRefundId: string }`

Add `packages/payment/src/registry.ts` with:
- `registerPaymentProvider(provider)`
- `getPaymentProvider(providerId)`
- `resetPaymentProviderRegistry()` for deterministic tests

Export the new surface from `packages/payment/src/index.ts` without removing the existing service exports.

Use the test file to lock the contract shape and registry behavior:
- resolving a registered provider by id succeeds
- missing provider throws a clear error
- reset clears the registry between tests

Do not add charge/capture abstractions in this phase. Keep the boundary tight around the refund execution Phase 10 actually needs.
  </action>
  <verify>
    <automated>cd /Users/d/Documents/Projects/turborepo-alchemy/packages/payment && bunx vitest run src/__tests__/cloudpayments-provider.test.ts && bun run check-types</automated>
  </verify>
  <done>
    `@my-app/payment` exposes a stable refund-provider contract and resettable registry that downstream workflows can depend on without importing provider-specific code.
  </done>
</task>

<task type="auto">
  <name>Task 2: Implement the CloudPayments refund adapter with org-scoped runtime config</name>
  <files>
    packages/payment/src/adapters/cloudpayments.ts
    packages/payment/src/__tests__/cloudpayments-provider.test.ts
    packages/payment/src/index.ts
  </files>
  <action>
Implement `packages/payment/src/adapters/cloudpayments.ts` using the new provider contract.

Requirements:
- Accept org-scoped config at call time rather than constructor-only secrets.
- Parse the stored credential blob into the provider-specific shape inside the adapter boundary. For Phase 10, do **not** build a new encryption subsystem; treat malformed credential blobs as an explicit adapter error.
- Use built-in `fetch` and Basic Auth against the CloudPayments refund endpoint.
- Convert integer cents to decimal provider amount only at the adapter boundary.
- Include the workflow/request idempotency key in the provider request where CloudPayments supports it.
- Return a stable `{ externalRefundId }` derived from the provider response.
- Throw explicit errors for malformed credentials, non-200 HTTP responses, or `Success: false` provider responses.

Add/extend tests for:
- successful refund request with parsed org credentials
- malformed credential blob / missing required fields
- non-success CloudPayments response
- integer-cent boundary preserved outside the adapter

Keep this adapter independent from disputes and handlers. It should be reusable by any later payment workflow.
  </action>
  <verify>
    <automated>cd /Users/d/Documents/Projects/turborepo-alchemy/packages/payment && bunx vitest run src/__tests__/cloudpayments-provider.test.ts && bun run check-types</automated>
  </verify>
  <done>
    The payment package can execute a real CloudPayments refund using org-scoped runtime config, and tests prove the adapter fails explicitly when credentials or provider responses are invalid.
  </done>
</task>

</tasks>

<verification>
1. `packages/payment` tests cover registry resolution/reset and CloudPayments refund execution behavior.
2. `packages/payment` type checks with the new provider boundary exported from `src/index.ts`.
</verification>

<success_criteria>
- A disputes or booking workflow can call `getPaymentProvider(...).refundPayment(...)` without importing CloudPayments directly.
- Org-scoped credentials are passed into the provider boundary at call time.
- CloudPayments refund execution is covered by unit tests and explicit failure modes.
</success_criteria>

<output>
After completion, create `.planning/phases/10-payment-webhook-cancellation-live-path/10-02-SUMMARY.md`
</output>
