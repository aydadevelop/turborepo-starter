---
phase: 07-review-missing-extractions
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/booking/src/action-policy.ts
  - packages/booking/src/overlap.ts
  - packages/booking/src/slots.ts
  - packages/booking/src/index.ts
  - packages/booking/src/__tests__/action-policy.test.ts
  - packages/booking/src/__tests__/overlap.test.ts
  - packages/pricing/src/pricing-profile.ts
  - packages/pricing/src/index.ts
autonomous: true
requirements:
  - EXTR-01
  - EXTR-02

must_haves:
  truths:
    - "Given a booking status and actor role, BookingActionPolicy returns whether cancellation/shift is allowed and how many hours before start the action is permitted"
    - "detectOverlap returns true when two time windows share any period; assertNoOverlap throws when an active booking occupies the requested slot for the same listing"
    - "calculateAvailableSlots returns time gaps that satisfy minimum duration rules given a set of busy intervals"
    - "resolveActivePricingProfile finds the listingPricingProfile row that is active (validFrom ≤ start, validTo > start or null, not archived) for a given listingId and date"
  artifacts:
    - path: packages/booking/src/action-policy.ts
      provides: BookingActionPolicy type, evaluateBookingActionWindow function
      exports:
        - BookingActionPolicy
        - evaluateBookingActionWindow
    - path: packages/booking/src/overlap.ts
      provides: detectOverlap, assertNoOverlap (db-backed)
      exports:
        - detectOverlap
        - assertNoOverlap
    - path: packages/booking/src/slots.ts
      provides: calculateAvailableSlots, findFreeGaps (pure computation)
      exports:
        - calculateAvailableSlots
        - findFreeGaps
        - BusyInterval
        - FreeGap
        - TimeSlot
    - path: packages/booking/src/__tests__/action-policy.test.ts
      provides: Unit tests for policy evaluation
    - path: packages/pricing/src/pricing-profile.ts
      provides: resolveActivePricingProfile (db-backed)
      exports:
        - resolveActivePricingProfile
  key_links:
    - from: packages/booking/src/overlap.ts
      to: "@my-app/db/schema/availability (listingAvailabilityBlock)"
      via: "import { listingAvailabilityBlock } from '@my-app/db/schema/availability'"
    - from: packages/booking/src/overlap.ts
      to: "@my-app/db/schema/marketplace (booking)"
      via: "import { booking } from '@my-app/db/schema/marketplace'"
    - from: packages/pricing/src/pricing-profile.ts
      to: "@my-app/db/schema/marketplace (listingPricingProfile, listingPricingRule)"
      via: "import { listingPricingProfile, listingPricingRule } from '@my-app/db/schema/marketplace'"
---

<objective>
Port the three load-bearing P0 booking computation services from the legacy repo into packages/booking (action-policy, overlap detection, slot calculation), and add resolveActivePricingProfile to packages/pricing.

Purpose: These are "gating functions" — createBooking and shift handlers need detectOverlap, and the checkout/quote flow needs resolveActivePricingProfile. Without them the booking path silently skips overlap checks and profile resolution.
Output: Four new source files + two test files; all exported from their package index.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@packages/booking/src/types.ts
@packages/booking/src/booking-service.ts
@packages/booking/src/index.ts
@packages/pricing/src/types.ts
@packages/pricing/src/pricing-service.ts
@packages/pricing/src/index.ts

<!-- Legacy source to port (read-only reference) -->
@legacy/full-stack-cf-app/packages/api/src/routers/booking/services/action-policy.ts
@legacy/full-stack-cf-app/packages/api/src/routers/booking/services/overlap.ts
@legacy/full-stack-cf-app/packages/api/src/routers/booking/services/slots.ts
@legacy/full-stack-cf-app/packages/api/src/routers/booking/services/pricing-profile.ts

<interfaces>
<!-- DB schema tables used by these services -->
<!-- From packages/db/src/schema/availability.ts -->
listingAvailabilityBlock  — columns: id, listingId, startsAt, endsAt, source, ...
listingAvailabilityRule   — columns: id, listingId, ...
listingMinimumDurationRule — columns: id, listingId, minimumDurationMinutes, daysOfWeek, startHour, startMinute, endHour, endMinute, isActive

<!-- From packages/db/src/schema/marketplace.ts -->
booking — columns: id, organizationId, listingId, startsAt, endsAt, status, ...
listingPricingProfile — columns: id, listingId, isDefault, validFrom, validTo, archivedAt, ...
listingPricingRule — columns: id, pricingProfileId, ...

<!-- Existing booking package Db type pattern -->
import type { db } from "@my-app/db";
export type Db = typeof db;

<!-- Blocking statuses already defined in booking-service.ts -->
const VALID_TRANSITIONS: Record<string, string[]> = { ... }
// Blocking statuses for overlap: pending, awaiting_payment, confirmed, in_progress
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Port action-policy + overlap into packages/booking</name>
  <files>
    packages/booking/src/action-policy.ts
    packages/booking/src/overlap.ts
    packages/booking/src/__tests__/action-policy.test.ts
    packages/booking/src/__tests__/overlap.test.ts
  </files>
  <action>
**Read the legacy files first:**
- `legacy/.../routers/booking/services/action-policy.ts` (153L)
- `legacy/.../routers/booking/services/overlap.ts` (87L)

**Port action-policy.ts** to `packages/booking/src/action-policy.ts`:
- Replace `import { db } from "@full-stack-cf-app/db"` with the `Db` type pattern: function parameters accept `db: Db` (do NOT use module-level singleton)
- Replace `@full-stack-cf-app/db/schema/auth` imports → `@my-app/db/schema/auth` (organization table)
- Remove `ORPCError` — throw `new Error("NOT_FOUND")` or `new Error("FORBIDDEN")` instead
- Export: `BookingActionPolicy`, `BookingActionPolicyAction`, `BookingActionPolicyActor`, `evaluateBookingActionWindow(params, db: Db): Promise<BookingActionPolicy>`

**Port overlap.ts** to `packages/booking/src/overlap.ts`:
- Replace `@full-stack-cf-app/db` → `@my-app/db`, `@full-stack-cf-app/db/schema/boat` → `@my-app/db/schema/availability` (`listingAvailabilityBlock`), `@full-stack-cf-app/db/schema/booking` → `@my-app/db/schema/marketplace` (`booking`)
- `boatId` → `listingId` in parameter names and where clauses
- Remove `ORPCError` — throw `new Error("OVERLAP_DETECTED")` instead
- Blocking statuses: `pending`, `awaiting_payment`, `confirmed`, `in_progress` (use the same set as in booking-service.ts)
- Export: `detectOverlap(params: { organizationId, listingId, startsAt, endsAt, excludeBookingId? }, db: Db): Promise<boolean>` and `assertNoOverlap` (throws on overlap)
- Inject `db: Db` parameter (no module-level DB)

**Write tests** in `__tests__/action-policy.test.ts` and `__tests__/overlap.test.ts`:
- action-policy: pure logic tests — mock the db query, test that evaluateBookingActionWindow returns correct allowed/denied result for different statuses and actors
- overlap: mock db query returning overlapping booking, assert detectOverlap returns true; mock returning empty, assert returns false
  </action>
  <verify>
    <automated>cd /Users/d/Documents/Projects/turborepo-alchemy && bun run --filter @my-app/booking test 2>&1 | tail -30</automated>
  </verify>
  <done>
    Both test files pass. action-policy.ts exports evaluateBookingActionWindow. overlap.ts exports detectOverlap and assertNoOverlap. Both accept db as a parameter (no module-level singleton).
  </done>
</task>

<task type="auto">
  <name>Task 2: Port slots.ts into packages/booking + add resolveActivePricingProfile to packages/pricing</name>
  <files>
    packages/booking/src/slots.ts
    packages/booking/src/index.ts
    packages/pricing/src/pricing-profile.ts
    packages/pricing/src/index.ts
  </files>
  <action>
**Read the legacy files first:**
- `legacy/.../routers/booking/services/slots.ts` (502L)
- `legacy/.../routers/booking/services/pricing-profile.ts` (37L)

**Port slots.ts** to `packages/booking/src/slots.ts`:
- This file is pure computation — no DB calls. It takes `BusyInterval[]`, `MinimumDurationRule[]`, and pricing data as inputs and returns `TimeSlot[]` or `FreeGap[]`
- Adapt any import from the legacy pricing helper (`./pricing`) to instead use the types defined in packages/booking/src/types.ts or defined locally in slots.ts
- Replace `boatId` → `listingId` in interfaces if present
- Export all interfaces: `BusyInterval`, `FreeGap`, `TimeSlot`, `AnnotatedTimeSlot`, `MinimumDurationRule`, and functions: `calculateAvailableSlots`, `findFreeGaps`
- No DB import needed

**Add resolveActivePricingProfile** to `packages/pricing/src/pricing-profile.ts` (new file):
- Port from `legacy/.../routers/booking/services/pricing-profile.ts`
- Adapt: `@full-stack-cf-app/db` → `@my-app/db`, `boatPricingProfile` → `listingPricingProfile` (from `@my-app/db/schema/marketplace`), `boatId` param → `listingId`, remove `ORPCError` → throw `new Error("NO_ACTIVE_PRICING_PROFILE")`
- Function signature: `resolveActivePricingProfile(params: { listingId: string; startsAt: Date }, db: Db): Promise<PricingProfileRow>`
- Inject `db: Db` parameter

**Update index.ts files:**
- `packages/booking/src/index.ts`: add `export * from "./action-policy"`, `export * from "./overlap"`, `export * from "./slots"`
- `packages/pricing/src/index.ts`: add `export * from "./pricing-profile"`
  </action>
  <verify>
    <automated>cd /Users/d/Documents/Projects/turborepo-alchemy && bun run --filter @my-app/booking typecheck 2>&1 | tail -20 && bun run --filter @my-app/pricing typecheck 2>&1 | tail -20</automated>
  </verify>
  <done>
    Both packages typecheck cleanly. packages/booking/src/index.ts exports slots.ts symbols. packages/pricing/src/index.ts exports resolveActivePricingProfile. No module-level DB singletons in any ported file.
  </done>
</task>

</tasks>

<verification>
After both tasks complete:
1. `bun run --filter @my-app/booking test` — all tests pass (including new action-policy + overlap tests)
2. `bun run --filter @my-app/booking typecheck` — no type errors
3. `bun run --filter @my-app/pricing typecheck` — no type errors
4. `grep -c "evaluateBookingActionWindow\|detectOverlap\|assertNoOverlap\|calculateAvailableSlots\|resolveActivePricingProfile" packages/booking/src/index.ts packages/pricing/src/index.ts` → ≥ 5 exports confirmed
</verification>

<success_criteria>
- packages/booking exports: evaluateBookingActionWindow, detectOverlap, assertNoOverlap, calculateAvailableSlots, findFreeGaps, BusyInterval, FreeGap, TimeSlot (from new files)
- packages/pricing exports: resolveActivePricingProfile
- All ported functions accept db as a parameter (no module-level DB imports)
- All schema references use @my-app/db naming (listingId, listingAvailabilityBlock, listingPricingProfile — NOT boat*)
- Test suite passes with ≥ 4 new tests
</success_criteria>

<output>
After completion, create `.planning/phases/07-review-missing-extractions/07-01-SUMMARY.md`
</output>
