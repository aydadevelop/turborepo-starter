---
phase: 04-availability-pricing-core
plan: 03
type: execute
wave: 2
depends_on:
  - "04-01"
  - "04-02"
files_modified:
  - packages/api-contract/src/routers/availability.ts
  - packages/api-contract/src/routers/pricing.ts
  - packages/api-contract/src/routers/index.ts
  - packages/api/src/handlers/availability.ts
  - packages/api/src/handlers/pricing.ts
  - packages/api/src/handlers/index.ts
  - packages/api/package.json
autonomous: true
requirements:
  - AVPR-01
  - AVPR-02
  - AVPR-03
  - AVPR-04

must_haves:
  truths:
    - "Operator can call addRule / deleteRule / listRules through typed oRPC procedures"
    - "Operator can call createProfile / updateProfile / listProfiles / addRule / deleteRule through typed oRPC procedures"
    - "Customer can call getQuote and receive QuoteBreakdown without authentication"
    - "If slot is unavailable, checkSlot returns { available: false } instead of an error"
    - "All handler files contain zero pricing or availability calculation logic — only delegation + translation"
  artifacts:
    - path: "packages/api-contract/src/routers/availability.ts"
      provides: "oRPC contract for availability procedures"
      exports: ["availabilityContract"]
    - path: "packages/api-contract/src/routers/pricing.ts"
      provides: "oRPC contract for pricing and quote procedures"
      exports: ["pricingContract"]
    - path: "packages/api/src/handlers/availability.ts"
      provides: "Thin handlers delegating to @my-app/availability"
    - path: "packages/api/src/handlers/pricing.ts"
      provides: "Thin handlers delegating to @my-app/pricing; translateUnknownError helper"
  key_links:
    - from: "packages/api/src/handlers/availability.ts"
      to: "@my-app/availability"
      via: "import { assertSlotAvailable, createAvailabilityRule, ... }"
      pattern: "import.*@my-app/availability"
    - from: "packages/api/src/handlers/pricing.ts"
      to: "@my-app/pricing"
      via: "import { calculateQuote, createPricingProfile, ... }"
      pattern: "import.*@my-app/pricing"
    - from: "packages/api-contract/src/routers/index.ts"
      to: "packages/api-contract/src/routers/availability.ts"
      via: "availability: availabilityContract"
      pattern: "availability.*availabilityContract"
---

<objective>
Wire the `@my-app/availability` and `@my-app/pricing` domain packages through thin oRPC transport handlers. Define contracts in `api-contract`, create thin handlers in `api`, and update both routers index files.

Purpose: Closes the transport gap for AVPR-01 through AVPR-04 — operators and customers can now call typed procedures; no business logic lives in handlers.
Output: Type-checked contracts and handlers; `bun run check-types` passes across api-contract and api.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-availability-pricing-core/04-RESEARCH.md
@.planning/phases/04-availability-pricing-core/04-01-SUMMARY.md
@.planning/phases/04-availability-pricing-core/04-02-SUMMARY.md

<interfaces>
<!-- oRPC contract pattern from packages/api-contract/src/routers/listing.ts -->
```typescript
import { oc } from "@orpc/contract";
import z from "zod";
// Procedures:
export const listingContract = {
  create: oc.route({ tags: ["Listings"] }).input(schema).output(schema),
  // ...
};
```

<!-- Handler pattern from packages/api/src/handlers/listing.ts -->
```typescript
import { organizationPermissionProcedure } from "../index";
export const listingRouter = {
  create: organizationPermissionProcedure({ listing: ["create"] })
    .listing.create.handler(async ({ context, input }) => {
      // delegate to domain service
    }),
};
```

<!-- publicProcedure pattern from packages/api/src/handlers/storefront.ts -->
```typescript
import { publicProcedure } from "../index";
import { ORPCError } from "@orpc/server";
export const storefrontRouter = {
  get: publicProcedure.storefront.get.handler(async ({ input }) => {
    try {
      const result = await getDomainData(input.id, db);
      return result;
    } catch (e) {
      if (e instanceof Error && e.message === "NOT_FOUND") throw new ORPCError("NOT_FOUND");
      throw e;
    }
  }),
};
```

<!-- Quick Zod patterns that work in Zod v4 -->
z.record(z.string(), z.unknown())  // metadata / conditionJson
z.string().datetime()              // ISO datetime input
z.number().int()                   // integer amounts
```

From @my-app/availability exports (after 04-01):
- createAvailabilityRule, deleteAvailabilityRule, listAvailabilityRules
- createAvailabilityBlock, deleteAvailabilityBlock
- createAvailabilityException, deleteAvailabilityException
- checkSlotAvailable, assertSlotAvailable

From @my-app/pricing exports (after 04-02):
- createPricingProfile, updatePricingProfile, listPricingProfiles
- createPricingRule, deletePricingRule
- calculateQuote, QuoteBreakdown
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: oRPC contracts for availability and pricing</name>
  <files>
    packages/api-contract/src/routers/availability.ts
    packages/api-contract/src/routers/pricing.ts
    packages/api-contract/src/routers/index.ts
  </files>
  <action>
    **packages/api-contract/src/routers/availability.ts**:

    ```typescript
    import { oc } from "@orpc/contract";
    import z from "zod";

    const availabilityRuleOutput = z.object({
      id: z.string(),
      listingId: z.string(),
      dayOfWeek: z.number().int().min(0).max(6),
      startMinute: z.number().int(),
      endMinute: z.number().int(),
      isActive: z.boolean(),
      createdAt: z.string().datetime(),
      updatedAt: z.string().datetime(),
    });

    const availabilityBlockOutput = z.object({
      id: z.string(),
      listingId: z.string(),
      source: z.string(),
      startsAt: z.string().datetime(),
      endsAt: z.string().datetime(),
      reason: z.string().nullable(),
      isActive: z.boolean(),
      createdAt: z.string().datetime(),
      updatedAt: z.string().datetime(),
    });

    const availabilityExceptionOutput = z.object({
      id: z.string(),
      listingId: z.string(),
      date: z.string(), // ISO date string
      isAvailable: z.boolean(),
      startMinute: z.number().int().nullable(),
      endMinute: z.number().int().nullable(),
      reason: z.string().nullable(),
      createdAt: z.string().datetime(),
      updatedAt: z.string().datetime(),
    });

    export const availabilityContract = {
      addRule: oc.route({ tags: ["Availability"], summary: "Add recurring availability rule" })
        .input(z.object({
          listingId: z.string(),
          dayOfWeek: z.number().int().min(0).max(6),
          startMinute: z.number().int().min(0).max(1440),
          endMinute: z.number().int().min(1).max(1440),
        }))
        .output(availabilityRuleOutput),

      deleteRule: oc.route({ tags: ["Availability"], summary: "Delete availability rule" })
        .input(z.object({ id: z.string() }))
        .output(z.object({ success: z.boolean() })),

      listRules: oc.route({ tags: ["Availability"], summary: "List availability rules" })
        .input(z.object({ listingId: z.string() }))
        .output(z.array(availabilityRuleOutput)),

      addBlock: oc.route({ tags: ["Availability"], summary: "Add availability block" })
        .input(z.object({
          listingId: z.string(),
          startsAt: z.string().datetime(),
          endsAt: z.string().datetime(),
          reason: z.string().optional(),
        }))
        .output(availabilityBlockOutput),

      deleteBlock: oc.route({ tags: ["Availability"], summary: "Delete availability block" })
        .input(z.object({ id: z.string() }))
        .output(z.object({ success: z.boolean() })),

      addException: oc.route({ tags: ["Availability"], summary: "Add availability exception" })
        .input(z.object({
          listingId: z.string(),
          date: z.string(), // ISO date YYYY-MM-DD
          isAvailable: z.boolean(),
          startMinute: z.number().int().optional(),
          endMinute: z.number().int().optional(),
          reason: z.string().optional(),
        }))
        .output(availabilityExceptionOutput),

      deleteException: oc.route({ tags: ["Availability"], summary: "Delete availability exception" })
        .input(z.object({ id: z.string() }))
        .output(z.object({ success: z.boolean() })),

      checkSlot: oc.route({ tags: ["Availability"], summary: "Check slot availability" })
        .input(z.object({
          listingId: z.string(),
          startsAt: z.string().datetime(),
          endsAt: z.string().datetime(),
        }))
        .output(z.object({ available: z.boolean() })),
    };
    ```

    **packages/api-contract/src/routers/pricing.ts**:

    ```typescript
    import { oc } from "@orpc/contract";
    import z from "zod";

    const pricingProfileOutput = z.object({
      id: z.string(),
      listingId: z.string(),
      name: z.string(),
      currency: z.string(),
      baseHourlyPriceCents: z.number().int(),
      minimumHours: z.number().int(),
      serviceFeeBps: z.number().int(),
      taxBps: z.number().int(),
      isDefault: z.boolean(),
      createdAt: z.string().datetime(),
      updatedAt: z.string().datetime(),
    });

    const pricingRuleOutput = z.object({
      id: z.string(),
      listingId: z.string(),
      pricingProfileId: z.string(),
      name: z.string(),
      ruleType: z.string(),
      conditionJson: z.record(z.string(), z.unknown()),
      adjustmentType: z.string(),
      adjustmentValue: z.number().int(),
      priority: z.number().int(),
      isActive: z.boolean(),
      createdAt: z.string().datetime(),
      updatedAt: z.string().datetime(),
    });

    const quoteBreakdownOutput = z.object({
      listingId: z.string(),
      profileId: z.string(),
      currency: z.string(),
      durationMinutes: z.number(),
      baseCents: z.number().int(),
      adjustmentCents: z.number().int(),
      serviceFeeCents: z.number().int(),
      taxCents: z.number().int(),
      totalCents: z.number().int(),
    });

    export const pricingContract = {
      createProfile: oc.route({ tags: ["Pricing"], summary: "Create pricing profile" })
        .input(z.object({
          listingId: z.string(),
          name: z.string().min(1),
          currency: z.string().length(3),
          baseHourlyPriceCents: z.number().int().positive(),
          minimumHours: z.number().int().min(1).optional(),
          serviceFeeBps: z.number().int().min(0).optional(),
          taxBps: z.number().int().min(0).optional(),
          isDefault: z.boolean().optional(),
        }))
        .output(pricingProfileOutput),

      updateProfile: oc.route({ tags: ["Pricing"], summary: "Update pricing profile" })
        .input(z.object({
          id: z.string(),
          name: z.string().min(1).optional(),
          baseHourlyPriceCents: z.number().int().positive().optional(),
          serviceFeeBps: z.number().int().min(0).optional(),
          taxBps: z.number().int().min(0).optional(),
          isDefault: z.boolean().optional(),
        }))
        .output(pricingProfileOutput),

      listProfiles: oc.route({ tags: ["Pricing"], summary: "List pricing profiles" })
        .input(z.object({ listingId: z.string() }))
        .output(z.array(pricingProfileOutput)),

      addRule: oc.route({ tags: ["Pricing"], summary: "Add pricing rule" })
        .input(z.object({
          listingId: z.string(),
          pricingProfileId: z.string(),
          name: z.string().min(1),
          ruleType: z.string().min(1),
          conditionJson: z.record(z.string(), z.unknown()),
          adjustmentType: z.enum(["percent", "flat_cents"]),
          adjustmentValue: z.number().int(),
          priority: z.number().int().optional(),
        }))
        .output(pricingRuleOutput),

      deleteRule: oc.route({ tags: ["Pricing"], summary: "Delete pricing rule" })
        .input(z.object({ id: z.string() }))
        .output(z.object({ success: z.boolean() })),

      getQuote: oc.route({ tags: ["Pricing"], summary: "Get quote for a slot" })
        .input(z.object({
          listingId: z.string(),
          startsAt: z.string().datetime(),
          endsAt: z.string().datetime(),
          passengers: z.number().int().positive().optional(),
        }))
        .output(quoteBreakdownOutput),
    };
    ```

    **packages/api-contract/src/routers/index.ts** — add to existing exports:
    ```typescript
    import { availabilityContract } from "./availability";
    import { pricingContract } from "./pricing";
    // add to the router object:
    availability: availabilityContract,
    pricing: pricingContract,
    ```

    Run: `bun run check-types --filter=@my-app/api-contract` → 0 errors.
  </action>
  <verify>
    ```bash
    bun run check-types --filter=@my-app/api-contract
    ```
    Zero errors.
  </verify>
  <done>Contracts defined for 8 availability and 6 pricing procedures. Both added to the main router index.</done>
</task>

<task type="auto">
  <name>Task 2: Thin handlers in packages/api</name>
  <files>
    packages/api/src/handlers/availability.ts
    packages/api/src/handlers/pricing.ts
    packages/api/src/handlers/index.ts
    packages/api/package.json
  </files>
  <action>
    First update `packages/api/package.json` — add:
    ```json
    "@my-app/availability": "workspace:*",
    "@my-app/pricing": "workspace:*"
    ```
    Run `bun install`.

    **packages/api/src/handlers/availability.ts**:

    All org-protected handlers use `organizationPermissionProcedure({ availability: [...] })`.
    `checkSlot` uses `publicProcedure` (no auth).

    - `addRule` → `organizationPermissionProcedure({ availability: ["create"] })`.availability.addRule.handler
      — calls `createAvailabilityRule({ listingId, organizationId: ctx.activeMembership.organizationId, dayOfWeek, startMinute, endMinute }, db)`
      — formats timestamps to ISO strings in return value

    - `deleteRule` → `organizationPermissionProcedure({ availability: ["delete"] })`.availability.deleteRule.handler
      — calls `deleteAvailabilityRule(input.id, ctx.activeMembership.organizationId, db)`
      — catches `Error("NOT_FOUND")` → `ORPCError("NOT_FOUND")`
      — returns `{ success: true }`

    - `listRules` → `organizationPermissionProcedure({ availability: ["read"] })`.availability.listRules.handler
      — calls `listAvailabilityRules(input.listingId, ctx.activeMembership.organizationId, db)`
      — formats each row's timestamps

    - `addBlock` → `organizationPermissionProcedure({ availability: ["create"] })`.availability.addBlock.handler
      — parse `startsAt/endsAt` from ISO string: `new Date(input.startsAt)`
      — calls `createAvailabilityBlock({ listingId, organizationId, startsAt, endsAt, reason }, db)`
      — format `startsAt/endsAt` timestamps in output

    - `deleteBlock` → same pattern as deleteRule

    - `addException` → `organizationPermissionProcedure({ availability: ["create"] })`.availability.addException.handler
      — parse date: `new Date(input.date)`
      — calls `createAvailabilityException({ listingId, organizationId, date, isAvailable, startMinute, endMinute, reason }, db)`
      — catches `Error("DUPLICATE_DATE")` → `ORPCError("CONFLICT")`

    - `deleteException` → same pattern

    - `checkSlot` → `publicProcedure`.availability.checkSlot.handler
      — calls `checkSlotAvailable(input.listingId, new Date(input.startsAt), new Date(input.endsAt), db)`
      — returns `{ available: result }` — no errors thrown, always returns the boolean

    **packages/api/src/handlers/pricing.ts**:

    All org-protected handlers use `organizationPermissionProcedure({ pricing: [...] })`.
    `getQuote` uses `publicProcedure`.

    Helper `formatProfile(row: PricingProfileRow)` → spreads row, converts timestamps to ISO.
    Helper `formatRule(row: PricingRuleRow)` → same.

    - `createProfile` → `organizationPermissionProcedure({ pricing: ["create"] })`.pricing.createProfile.handler
    - `updateProfile` → `organizationPermissionProcedure({ pricing: ["update"] })`.pricing.updateProfile.handler
    - `listProfiles` → `organizationPermissionProcedure({ pricing: ["read"] })`.pricing.listProfiles.handler
    - `addRule` → `organizationPermissionProcedure({ pricing: ["create"] })`.pricing.addRule.handler
    - `deleteRule` → `organizationPermissionProcedure({ pricing: ["delete"] })`.pricing.deleteRule.handler
      — catches `Error("NOT_FOUND")` → `ORPCError("NOT_FOUND")`; returns `{ success: true }`

    - `getQuote` → `publicProcedure`.pricing.getQuote.handler
      — calls `calculateQuote({ listingId, startsAt: new Date(input.startsAt), endsAt: new Date(input.endsAt), passengers: input.passengers }, db)`
      — catches `Error("NO_PRICING_PROFILE")` → `ORPCError("NOT_FOUND", { message: "No pricing profile found for this listing" })`

    **packages/api/src/handlers/index.ts** — add:
    ```typescript
    import { availabilityRouter } from "./availability";
    import { pricingRouter } from "./pricing";
    // in the router object:
    availability: availabilityRouter,
    pricing: pricingRouter,
    ```

    Run:
    ```bash
    bun install
    bun run check-types --filter=@my-app/api
    ```
  </action>
  <verify>
    ```bash
    bun run check-types --filter=@my-app/api-contract
    bun run check-types --filter=@my-app/api
    ```
    Both pass with zero errors.
  </verify>
  <done>
    Availability and pricing handlers wired. All 4 packages type-check cleanly.
    Handlers contain zero pricing/availability logic — only delegation and error translation.
  </done>
</task>

</tasks>

<verification>
```bash
bun run check-types --filter=@my-app/api-contract
bun run check-types --filter=@my-app/api
bun run test --filter=@my-app/availability
bun run test --filter=@my-app/pricing
bun run test --filter=@my-app/auth
```
All pass.
</verification>

<success_criteria>
- 8 availability procedures and 6 pricing procedures accessible through typed oRPC contracts
- `getQuote` and `checkSlot` use `publicProcedure` (no auth required)
- All protected procedures use `organizationPermissionProcedure` with correct resource permissions
- Handlers contain NO calculation logic — pure delegation to domain packages
- `ORPCError("NOT_FOUND")` for domain `Error("NOT_FOUND")`, `ORPCError("CONFLICT")` for slot/date conflicts
- Zero TypeScript errors across all 4 packages
</success_criteria>

<output>
After completion, create `.planning/phases/04-availability-pricing-core/04-03-SUMMARY.md`.
</output>
