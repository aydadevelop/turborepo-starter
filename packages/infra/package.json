{
  "name": "@my-app/infra",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "alchemy dev --stage ${STAGE:-dev}",
    "deploy": "NODE_TLS_REJECT_UNAUTHORIZED=0 alchemy deploy",
    "destroy": "NODE_TLS_REJECT_UNAUTHORIZED=0 alchemy destroy",
    "check-types": "tsc --noEmit -p tsconfig.json",
    "lint": "cd ../.. && biome check --config-path biome.json packages/infra",
    "lint:fix": "cd ../.. && biome check --config-path biome.json packages/infra --write"
  },
  "dependencies": {
    "dotenv": "^17.2.2",
    "zod": "^4.1.13"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.4.4",
    "@my-app/config": "*",
    "@my-app/proxy": "*",
    "alchemy": "0.85.1",
    "typescript": "^5",
    "ultracite": "^7.2.3"
  }
}
