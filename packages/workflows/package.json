{
  "name": "@my-app/workflows",
  "type": "module",
  "exports": {
    ".": {
      "default": "./src/index.ts"
    }
  },
  "scripts": {
    "check-types": "tsc --noEmit -p tsconfig.json",
    "lint": "cd ../.. && biome check --config-path biome.json packages/workflows",
    "lint:fix": "cd ../.. && biome check --config-path biome.json packages/workflows --write",
    "test": "vitest run --passWithNoTests",
    "test:watch": "vitest --watch"
  },
  "dependencies": {
    "@my-app/events": "workspace:*",
    "@my-app/db": "workspace:*",
    "drizzle-orm": "1.0.0-beta.18-5ffac6d"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.4.6",
    "@my-app/config": "workspace:*",
    "@my-app/vitest-config": "workspace:*",
    "typescript": "^5",
    "vitest": "^4.0.18"
  }
}
