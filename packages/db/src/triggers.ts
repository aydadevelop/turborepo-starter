/**
 * Reserved for SQLite triggers applied after Drizzle migrations.
 *
 * Keep this export so test/bootstrap callers can remain stable while the
 * starter schema avoids domain-specific trigger logic.
 */
export const POST_MIGRATION_TRIGGERS_SQL = "";
