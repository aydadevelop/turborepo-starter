/**
 * Reserved for Postgres triggers/functions applied after Drizzle migrations.
 *
 * Keep this export so test/bootstrap callers can remain stable.
 */
export const POST_MIGRATION_TRIGGERS_SQL = "";
