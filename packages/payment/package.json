{
  "name": "@my-app/payment",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": {
      "default": "./src/index.ts"
    },
    "./webhooks": {
      "default": "./src/webhooks/index.ts"
    },
    "./webhooks/*": {
      "default": "./src/webhooks/*.ts"
    }
  },
  "scripts": {
    "check-types": "tsc --noEmit -p tsconfig.json",
    "lint": "cd ../.. && biome check --config-path biome.json packages/payment",
    "lint:fix": "cd ../.. && biome check --config-path biome.json packages/payment --write",
    "test": "vitest run --passWithNoTests",
    "test:watch": "vitest --watch"
  },
  "dependencies": {
    "@my-app/db": "workspace:*",
    "@my-app/events": "workspace:*",
    "drizzle-orm": "1.0.0-beta.16-2ffd1a5",
    "zod": "^4.1.13"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.4.7",
    "@my-app/config": "workspace:*",
    "@my-app/vitest-config": "workspace:*",
    "typescript": "^5",
    "vitest": "^4.0.18"
  }
}
