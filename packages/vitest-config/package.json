{
  "name": "@full-stack-cf-app/vitest-config",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "check-types": "tsc --noEmit -p tsconfig.json",
    "lint": "cd ../.. && biome check --config-path biome.json packages/vitest-config",
    "lint:fix": "cd ../.. && biome check --config-path biome.json packages/vitest-config --write"
  },
  "dependencies": {
    "vitest": "^3.2.4"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.3.13",
    "@full-stack-cf-app/config": "*",
    "typescript": "^5",
    "ultracite": "^7.1.3"
  }
}
